# PlotBetter 使用说明（v0.1）

PlotBetter（PB）是一个 **桌面小助手**：你在记事本里写 daily 笔记，PB 在窗口里显示任务，并按主题推送论文、出测验题。

---

## 一、第一次启动

### 需要准备

- **Windows 10 / 11**
- **Node.js**（你电脑上已有 v22，无需再装）
- **网络**（推送论文需要访问 arXiv）

### 启动步骤

1. 打开文件夹 `D:\PlotBetter`
2. **双击 `PlotBetter.exe`**，或双击桌面上的 **PlotBetter** 快捷方式
3. 如果依赖尚未安装，`PlotBetter.exe` 会询问是否自动安装；完成后出现 PlotBetter 窗口
4. 系统托盘（右下角）会出现 PB 图标；双击可重新打开窗口

> 桌面和开始菜单快捷方式都已创建；以后双击快捷方式即可直接启动。
> 需要重新创建快捷方式时，运行：
>
> ```powershell
> D:\PlotBetter\PlotBetter.exe --install-shortcut
> ```
>
> 删除快捷方式：
>
> ```powershell
> D:\PlotBetter\PlotBetter.exe --remove-shortcut
> ```
>
> `run.bat` 仍保留为备用启动方式；若它闪退，可右键「以管理员身份运行」或在文件夹地址栏输入 `cmd`，执行 `npm install` 后再 `npm start`。

---

## 二、Daily 笔记在哪里？

默认文件路径：

```
D:\PlotBetter\data\daily.txt
```

在 PB 窗口左下角点 **「Open daily note」**，会用系统默认程序打开（通常是记事本）。

**你可以一直用记事本编辑这个文件**，PB 会自动检测变化并更新界面。

---

## 三、怎么写 daily 笔记？

### 1. 定义研究主题

每行一个，英文关键词效果更好（论文来自 arXiv）：

```text
@topic: machine learning
@topic: attention mechanism
```

PB 会读取这些行，用于 **论文推送** 和 **认知测验**。

### 2. 写任务

```text
- [ ] Read today's paper recommendation
- [ ] Take the cognitive quiz
- [x] Already finished task
```

- `- [ ]` = 未完成（会显示在 Tasks 页）
- `- [x]` = 已完成（会划掉）

在 PB 里勾选任务，也会同步写回 `daily.txt`。

---

## 四、界面说明

| 页面 | 作用 |
|------|------|
| **Daily Note** | 内置记事本，直接编辑 daily 笔记（自动保存） |
| **Tasks** | 今日待办：添加、编辑、勾选完成 |
| **All Tasks** | 全部已完成任务，按日期归档 |
| **Today's Paper** | 按主题每天推荐 1 篇英文论文 |
| **Quiz** | 做一道选择题，更新认知等级（1～5） |
| **Topics** | 添加 / 删除研究主题 |
| **Settings** | 可选：关联 Zotero 文献库 |

### Daily Note（推荐日常使用）

打开软件后默认进入 **Daily Note** 页，像记事本一样直接输入即可：

- 输入会自动保存（约 1 秒后），也可按 **Ctrl+S** 或点 **Save**
- 右上角显示 **Saved** / **Unsaved changes**
- 仍可用左下角 **Open in Notepad** 用系统记事本打开同一文件

```text
@topic: machine learning

- [ ] Read today's paper
- [x] Finished yesterday's task
```

### Tasks / All Tasks

- **Tasks**（今日任务）：只显示今天待办；勾选完成会移入 **All Tasks**
- **All Tasks**（全部任务）：与 Settings 同级，独立页面；按完成日期分组展示历史记录

在 **Tasks** 勾选任务，或于 Daily Note 写 `- [x]` 保存后，记录会出现在 **All Tasks** 对应日期下。可点 × 删除某条历史记录。

### Topics（图形化编辑）

### Today's Paper 怎么用？

1. 顶部选择主题（来自 `@topic`）
2. 点 **Load / Refresh**
3. 阅读英文标题、摘要、要点
4. 点 **View in Zotero** — 保存到 `daily_paper` 收藏夹并在 Zotero 中打开（需先在 Settings 配置 API Key）
5. 或点 **Open on arXiv** 在浏览器打开
6. 读完后点反馈：**Too hard** / **Just right** / **Too easy**
   - 太难 → 等级降低，以后推更基础的文  
   - 太简单 → 等级升高，以后推更深入的文  

每个主题 **每天只推 1 篇**（同一天重复打开会显示同一篇）。

### Quiz 怎么用？

1. 选择主题 → 点 **New question**
2. 选 A/B/C/D（英文题）
3. 答对等级 +1，答错等级 -1（最高 5，最低 1）

### Settings：关联 Zotero（可选）

若你使用 [Zotero](https://www.zotero.org/) 管理文献，可在 **Settings** 页开启集成：

1. 勾选 **Enable Zotero integration**
2. （推荐）勾选 **Skip papers already in my Zotero library** — 推荐论文时会跳过库中已有的 arXiv 文献
3. 填写 **Zotero User ID** 和 **API Key**（在 [zotero.org/settings/keys](https://www.zotero.org/settings/keys) 创建；需勾选 **Allow library write access**）
4. **Zotero data folder** 填 `D:\Zotero`（含 `zotero.sqlite` 的文件夹）
5. **Daily paper collection** 默认为 `daily_paper`（View in Zotero 时论文存入此收藏夹）
6. 点 **Test connection** → **Save settings**

**关联后能做什么：**

| 功能 | 说明 |
|------|------|
| **View in Zotero** | 保存到 `daily_paper` 收藏夹，并用 Zotero 桌面版打开 |
| 去重推荐 | 已收录的 arXiv 论文不再重复推送 |
| 自动入库 | 勾选 **Auto-add when "Just right"** 后，也会存入 `daily_paper` |

> **View in Zotero** 需要 API Key；本地路径 `D:\Zotero` 用于去重检测。

---

## 五、典型一天流程

```text
早上：打开 PlotBetter → 看 Tasks 今日待办
      ↓
      打开 daily.txt，确认 @topic 和任务
      ↓
      Today's Paper → 读一篇推荐论文
      ↓
      点 Too hard / Just right / Too easy
      ↓
      Quiz → 做 1 题更新认知
      ↓
晚上：在 daily.txt 把完成的任务改成 - [x]
```

---

## 六、数据存在哪？

| 文件 | 内容 |
|------|------|
| `data/daily.txt` | 你的笔记与任务（主文件，请备份） |
| `data/plotbetter.db` | 主题等级、已推论文、测验记录 |
| `data/config.json` | 配置（daily 文件路径等） |

---

## 七、常见问题

**Q：改了 daily.txt 但界面没更新？**  
点左下角 **Refresh**，或保存文件后等 1～2 秒。

**Q：论文搜不到？**  
把 `@topic` 改成更常见的英文学术词，如 `deep learning` 而不是过窄的中文译名。

**Q：没有网络能用吗？**  
Tasks 和 Topics 可以；论文推送需要联网。

**Q：如何退出？**  
关窗口不会退出；右键托盘图标 → **Quit**。

**Q：如何开机自启？**  
Win+R → `shell:startup` → 把 `PlotBetter.exe` 的快捷方式放进去；
也可以直接用 PB 窗口 Settings 里的 *Launch PlotBetter when Windows starts*。

---

## 八、本版尚未包含（后续可做）

- 多 daily 文件切换
- 邮件/通知提醒
- AI 生成更个性化的论文说明
- 与 Obsidian 等笔记软件联动

---

## 九、命令行启动（可选）

```bat
cd D:\PlotBetter
npm install
npm start
```

---

如有问题，保留 `data/daily.txt` 和报错截图即可排查。
