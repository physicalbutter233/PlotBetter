# 从“线线角”推导万物

---

## 零、 核心基石：平面的“一维代言人”（法向量）

### 1. 为什么必须引入法向量？

在空间中，“直线”是一维的，我们用一个方向向量 $\vec{s} = (x, y, z)$ 就能把它的方向定死；但“平面”是二维的，它向四周无限发散，**你在平面内随便画两个向量，它们的方向都是不一样的**。我们无法用平面内的某一个向量来代表整个平面。

为了解决这个“维度不对称”的数学困境，解析几何采取了降维策略：**在欧氏空间中，任何一个平面 $\alpha$，都有且仅有一个方向与它绝对垂直。** 我们把垂直于平面 $\alpha$ 的向量，称为该平面的**法向量（Normal Vector）**，记为 $\vec{n}$。**法向量，就是二维平面在向量世界里的“一维代言人”。**

### 2. 法向量的计算方式

设平面 $\alpha$ 内有两个**不共线**的向量 $\vec{a} = (x_1, y_1, z_1)$ 与 $\vec{b} = (x_2, y_2, z_2)$，求法向量 $\vec{n} = (x, y, z)$：

#### 方法一：待定系数赋元法（高中教材标准解法）

因为法向量垂直于平面内的所有向量，所以必须同时满足：
$$\begin{cases} \vec{n} \cdot \vec{a} = 0 \\ \vec{n} \cdot \vec{b} = 0 \end{cases} \implies \begin{cases} x_1x + y_1y + z_1z = 0 \\ x_2x + y_2y + z_2z = 0 \end{cases}$$

这是一个“2个方程求3个未知数”的方程组，解法固定为三步：
1. **消元**：把其中两个字母，全部用第三个字母表示（例如解出 $x = -2y,\; z = 3y$）；
2. **赋元**：**令作为“桥梁”的那个字母等于 $1$**（令 $y=1$）；
3. **得解**：直接写出 $\vec{n} = (-2, 1, 3)$。

#### 方法二：向量叉乘法（大学公式 · 草稿纸降维速算）

高等代数中，两向量的外积 $\vec{n} = \vec{a} \times \vec{b}$ 得到的新向量，天然垂直于 $\vec{a}$ 和 $\vec{b}$。口诀为**“双写掐头去尾，交叉相乘相减”**：

1. 把 $\vec{a}, \vec{b}$ 的坐标上下对齐，连写两遍：
   $$\begin{matrix} x_1 & y_1 & z_1 & x_1 & y_1 & z_1 \\ x_2 & y_2 & z_2 & x_2 & y_2 & z_2 \end{matrix}$$
2. 划掉最左边一列（掐头），划掉最右边一列（去尾）；
3. 剩下中间三组，从左至右依次执行 `(左上×右下) - (左下×右上)`：
   * $x = y_1z_2 - z_1y_2$
   * $y = z_1x_2 - x_1z_2$
   * $z = x_1y_2 - y_1x_2$

*(注：高考大题卷面上不可写叉乘符号 $\times$，建议在草稿纸上4秒算完，再回卷面上伪装成方法一的解方程步骤)*。

---

## 一、 元始母体：线线角公式

设空间中两根直线的方向向量分别为 $\vec{s}_1$ 与 $\vec{s}_2$，它们之间自由夹角（向量夹角）为 $\phi$。

高一平面向量基本定理规定：$\vec{s}_1 \cdot \vec{s}_2 = |\vec{s}_1| |\vec{s}_2| \cos\phi$。将其移项，得到**几何公理母式**：

$$\cos\phi = \frac{\vec{s}_1 \cdot \vec{s}_2}{|\vec{s}_1| |\vec{s}_2|}$$

> **空间公理化修正**：  
> 向量有方向，夹角 $\phi \in [0, \pi]$；但空间中两条**直线**是没有正负方向的，几何学规定**两直线夹角 $\theta \in [0, \frac{\pi}{2}]$（只取锐角或直角）**。  
> 当两向量夹角算出来是钝角（$\cos\phi < 0$）时，直线的夹角应当取它的补角。根据诱导公式 $\cos(\pi - \phi) = -\cos\phi = |\cos\phi|$，故母体公式套上绝对值：
> 
> $$\cos\theta = \frac{|\vec{s}_1 \cdot \vec{s}_2|}{|\vec{s}_1| |\vec{s}_2|}$$

---

## 二、 角度派生篇

### 派生一：面面角（二面角 $\theta \in [0, \pi]$）从何而来？

* **几何任务**：计算平面 $\alpha$ 与平面 $\beta$ 的夹角。
* **维度困境**：面与面无法做点积。
* **降维推导**：
  让平面 $\alpha$ 的代言人 $\vec{n}_1$ 与平面 $\beta$ 的代言人 $\vec{n}_2$ 代为出战。
  
  请在脑中构建这个动态过程：把一个笔记本合上，此时两封面的夹角是 $0^\circ$，它们的法向量互相平行；当你把笔记本像扇子一样缓缓打开，**封面转过了多少度，垂直插在封面上的法向量刚好也严格同步转过了多少度。**

  因此：**二面角的大小，完全等价于两平面法向量的夹角。** 直接套用母体公式：

  $$\cos\theta = \pm \frac{\vec{n}_1 \cdot \vec{n}_2}{|\vec{n}_1| |\vec{n}_2|}$$

  *(细节释疑：为什么这里不加绝对值而是写 $\pm$？因为二面角在几何定义里**分锐角和钝角**，它的余弦值允许为负。算完点积数值后，必须抬头看原图：图上张开的是锐角就取正值，张开的是钝角强行取负值)*。

***

### 派生二：线面角（$\theta \in [0, \frac{\pi}{2}]$）从何而来？

* **几何任务**：求直线 $l$（方向向量 $\vec{s}$）与平面 $\alpha$（法向量 $\vec{n}$）的夹角 $\theta$。
* **降维推导**：
  1. **先算母体角**：拿直线向量 $\vec{s}$ 直接去点乘平面的法向量 $\vec{n}$，根据母体公式，算出来的是**直线与法线的夹角**，记为 $\phi$：
     $$\cos\phi = \frac{|\vec{s} \cdot \vec{n}|}{|\vec{s}| |\vec{n}|}$$
  2. **观察直角几何关系**：法线垂直于平面。在空间中，**【斜线 $l$】、【斜线在平面上的投影】、【平面的法线】三者首尾相连，必定构成一个标准直角三角形**。
  3. 在直角三角形中，锐角互余：
     $$\text{线法夹角 } \phi + \text{线面夹角 } \theta = \frac{\pi}{2} \implies \phi = \frac{\pi}{2} - \theta$$
  4. 对上式两边同时套上余弦 $\cos$：
     $$\cos\phi = \cos\left(\frac{\pi}{2} - \theta\right) = \sin\theta$$

  **将上式等号右边的 $\sin\theta$，替换掉第1步里的 $\cos\phi$**，线面角公式当场破茧：

  $$\sin\theta = \frac{|\vec{s} \cdot \vec{n}|}{|\vec{s}| |\vec{n}|}$$


---

## 三、 距离派生篇

在高等数学视角下：**空间中任意点到几何体的垂直距离，本质上全部是“某条连线向量”在“垂直方向基底”上的正交投影。**

### 派生三：点到平面的距离 $d$ 从何而来？

* **已知条件**：平面外一点 $P$，平面 $\alpha$ 的法向量为 $\vec{n}$。

* **推导步骤**：
  1. **建立连线**：在平面 $\alpha$ 上**随便挑一个已知坐标的点 $A$**，连接得到向量 $\vec{PA}$（这是一条斜线）。
  2. **锁定母夹角**：设斜线 $\vec{PA}$ 与平面法向量 $\vec{n}$ 的夹角为 $\phi$，代入母体公式：
     $$\cos\phi = \frac{|\vec{PA} \cdot \vec{n}|}{|\vec{PA}| |\vec{n}|}$$
  3. **看几何投影**：过点 $P$ 向平面作垂线段 $PO=d$。在直角 $\triangle POA$ 中，**垂直距离 $d$ 恰好是斜边 $PA$ 在法线方向上的“邻边”**：
     $$d = |\vec{PA}| \cdot \cos\phi$$
  4. **代数融合**：把第2步的 $\cos\phi$ 强行塞进第3步：
     $$d = |\vec{PA}| \cdot \left( \frac{|\vec{PA} \cdot \vec{n}|}{|\vec{PA}| |\vec{n}|} \right)$$

  **奇迹发生**：分子分母上的斜线模长 $|\vec{PA}|$ **当场约分抵消**！点面距离公式诞生：

  $$d = \frac{|\vec{PA} \cdot \vec{n}|}{|\vec{n}|}$$



### 派生四：点到直线的距离 $d$ 从何而来？

* **已知条件**：直线外一点 $P$，直线 $l$ 的方向向量为 $\vec{s}$，直线上已知锚点 $A$。

* **推导步骤**：
  1. **构造连线**：连接 $AP$ 得到斜线向量 $\vec{AP}$。
  2. **找母夹角**：设斜线 $\vec{AP}$ 与直线方向 $\vec{s}$ 的线线角为 $\theta$，代入母体：
     $$\cos\theta = \frac{|\vec{AP} \cdot \vec{s}|}{|\vec{AP}| |\vec{s}|}$$
  3. **看几何投影**：在直角 $\triangle P-A-\text{垂足}$ 中，点到直线的距离 $d$ 是角 $\theta$ 所对的**对边**：
     $$d = |\vec{AP}| \cdot \sin\theta$$
  4. 根据勾股恒等式 $\sin\theta = \sqrt{1 - \cos^2\theta}$，展开注入母体：
     $$d = |\vec{AP}| \cdot \sqrt{1 - \left( \frac{\vec{AP} \cdot \vec{s}}{|\vec{AP}| |\vec{s}|} \right)^2} = \frac{\sqrt{|\vec{AP}|^2 |\vec{s}|^2 - (\vec{AP} \cdot \vec{s})^2}}{|\vec{s}|}$$


